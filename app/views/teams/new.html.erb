<div class="col-md-4 col-md-offset-4">

<% if @team.errors.any? %>
  <div id="error_explanation">
  <div class = "alert alert-danger"
    <h2><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h2>

    <ul>
    <% @team.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  </div>
<% end %>

<h2> Add a New team </h2>
<br>
<div class="form-group">
<%= form_for @team, html: {class: "form"} do |f| %>

   <%= f.text_field :title, :placeholder => "Team Name", :class => "form-control"%>
   <%= f.text_field :desc, :placeholder => "Team Description", :class => "form-control" %>
   <%= f.text_field :loc, :placeholder => "Team Location", :class => "form-control" %>

   <%= f.submit :class=>'btn btn-primary'%>

<% end %>

<%= link_to 'Back', teams_path %>

</div>
</div>
</div>
</div>
